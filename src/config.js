/**
 * Configuration Module
 * Centralizes application constants and feature flags.
 */

export const STORAGE_MODE = {
    LOCAL: 'local',
    VAULT: 'vault'
};

export const THEME = {
    DARK: 'dark',
    LIGHT: 'light'
};

export const STORAGE_KEYS = {
    MODE: 'storageMode',
    THEME: 'theme'
};
